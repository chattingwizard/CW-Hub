# ===================================================
# CW Hub — Environment Variables
# ===================================================
# Copy this file to .env and fill in the values.
# NEVER commit .env — it is gitignored.
#
# The frontend works without any of these (Supabase
# anon key is in src/lib/supabase.ts by design).
# These are only needed for pipeline scripts.
# ===================================================

# --- Supabase ---
SUPABASE_URL=https://bnmrdlqqzxenyqjknqhy.supabase.co
SUPABASE_ANON_KEY=                # Public anon key (also hardcoded in src/lib/supabase.ts)
SUPABASE_SERVICE_KEY=             # service_role key — ask Pau via DM
SUPABASE_ACCESS_TOKEN=            # Management API token — ask Pau via DM

# --- Airtable (for pipeline/sync_airtable.py) ---
AIRTABLE_TOKEN=                   # Personal access token from airtable.com/create/tokens
AIRTABLE_BASE_ID=appy0qGaMEfyDz9LZ
AIRTABLE_DAILY_TABLE_ID=tbltaM187ka2UNOsi

# --- Hubstaff (for pipeline/sync_hubstaff.py) ---
HUBSTAFF_TOKEN=                   # JWT from Hubstaff — ask Pau via DM

# --- Slack (for automated messages) ---
SLACK_USER_TOKEN=                 # xoxp-... user token — ask Pau via DM

# --- Monday.com (for task sync - future) ---
MONDAY_API_TOKEN=                 # API token from monday.com

# --- Notion (for knowledge base sync - future) ---
NOTION_TOKEN=                     # ntn_... integration token
